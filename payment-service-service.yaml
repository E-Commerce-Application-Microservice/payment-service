apiVersion: v1
kind: Service
metadata:
  name: payment-service
  namespace: clahanstore
spec:
  clusterIP: None
  selector:
    app: payment-service
  ports:
  - port: 3009
    targetPort: 3009
---
apiVersion: v1
kind: Service
metadata:
  name: invoice-service
  namespace: clahanstore
spec:
  clusterIP: None
  selector:
    app: invoice-service
  ports:
  - port: 3010
    targetPort: 3010
